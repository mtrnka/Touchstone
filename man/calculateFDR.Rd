% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdr.R
\name{calculateFDR}
\alias{calculateFDR}
\title{Calculate FDR for a dataset above inter and intra-protein score thresholds}
\usage{
calculateFDR(
  datTab,
  threshold = list(intraThresh = -100, interThresh = -100),
  classifier = .data$SVM.score,
  scalingFactor = the$decoyScalingFactor
)
}
\arguments{
\item{datTab}{Parsed CLMS search results.}

\item{threshold}{A list or a numeric of length 1. List must contain either \code{globalThresh} or both \code{interThresh} and \code{intraThresh} numeric elements.}

\item{classifier}{Column name in \code{datTab} used as the classifier to use to rank hits.}

\item{scalingFactor}{An integer k. The multiple by which decoy DB is larger than target DB}
}
\value{
A numeric value
}
\description{
\code{calculateFDR()} estimates the FDR for a dataset based on counting the number
of decoy hits. Treats inter- and intra-protein hits separately. If the
parameter \code{separateThresh} is provided it first thresholds the data.
}
