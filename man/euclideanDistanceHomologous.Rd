% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distModFile.R
\name{euclideanDistanceHomologous}
\alias{euclideanDistanceHomologous}
\title{Measure Euclidean distance between C-alpha atoms for homologous chain mappings}
\usage{
euclideanDistanceHomologous(
  XLink.AA.1,
  PDB.chain.1,
  XLink.AA.2,
  PDB.chain.2,
  parsedPDB,
  exclude.self = TRUE
)
}
\arguments{
\item{XLink.AA.1}{Sequence index of the first crosslinked residue.}

\item{PDB.chain.1}{PDB chain identifier(s) for the first residue. Multiple
possible chains should be supplied as a comma-separated string.}

\item{XLink.AA.2}{Sequence index of the second crosslinked residue.}

\item{PDB.chain.2}{PDB chain identifier(s) for the second residue. Multiple
possible chains should be supplied as a comma-separated string.}

\item{parsedPDB}{A parsed Touchstone PDB table, usually generated by
\code{\link[=parsePDB]{parsePDB()}}. Must contain columns \code{chain}, \code{resno}, \code{x}, \code{y}, and \code{z}.}

\item{exclude.self}{Logical. If \code{TRUE}, candidate pairs where the same atom is
linked to itself are excluded. Default is \code{TRUE}.}
}
\value{
A list with elements \code{min.dist} and \code{chimera.coord}.
}
\description{
Calculates the shortest C-alpha to C-alpha distance for a crosslinked residue
pair when one or both residues can map to multiple homologous chains.
}
\details{
\code{PDB.chain.1} and \code{PDB.chain.2} may be comma-separated character strings.
For example, if the first residue may map to chains A, D, or G, then
\code{PDB.chain.1} should be \code{"A,D,G"}. All possible chain combinations are
evaluated, and the shortest valid distance is returned.

When \code{exclude.self = TRUE}, candidate pairs where the same atom is linked to
itself are excluded. Thus, if residue 144 can map to chains G and L, a
crosslink from residue 144 to residue 144 will be assigned to a cross-chain
pair such as \verb{/G:144@CA /L:144@CA}, rather than \verb{/G:144@CA /G:144@CA}.
}
\seealso{
\code{\link[=parsePDB]{parsePDB()}}, \code{\link[=euclideanHelper]{euclideanHelper()}}
}
