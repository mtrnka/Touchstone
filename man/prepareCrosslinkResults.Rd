% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/results.R
\name{prepareCrosslinkResults}
\alias{prepareCrosslinkResults}
\title{Prepare crosslink results at a reporting level}
\usage{
prepareCrosslinkResults(
  x,
  summarizationLevel = "urp",
  model = "selected",
  targetER = NULL,
  scalingFactor = NULL,
  retainGroups = TRUE,
  classifier = "SVM.score",
  thresholds = NULL
)
}
\arguments{
\item{x}{A \code{touchstone_training} result or a CSM-level data frame containing
the requested \code{classifier} column.}

\item{summarizationLevel}{Reporting level. One of \code{"csm"}, \code{"urp"},
\code{"peptide-pair"}, \code{"protein-pair"}, or \code{"module-pair"}.}

\item{model}{For a training result, \code{"selected"} or \code{"linear"} for the
recommended linear model, \code{"radial"} for the recommended radial model, or
a numeric candidate index. Ignored for a CSM data frame.}

\item{targetER}{Desired FDR. Defaults to the value stored in a training result,
or 0.01 for a data frame.}

\item{scalingFactor}{Multiple by which the decoy database is larger than the
target database. Defaults to the value stored in a training result, or the
value established by \code{\link[=setDecoyScalingFactor]{setDecoyScalingFactor()}} for a data frame.}

\item{retainGroups}{Retain existing data-frame groups during pair
summarization; passed to the existing \code{best*Pair()} functions.}

\item{classifier}{Column in the CSM table used to rank, summarize, threshold,
and classify hits. Defaults to \code{"SVM.score"}; alternatives such as
\code{"Score.Diff"} or an experimental score column are supported.}

\item{thresholds}{Optional manually selected threshold: one numeric global
threshold, a list containing \code{globalThresh}, or a list containing both
\code{interThresh} and \code{intraThresh}. When omitted, thresholds are estimated
with \code{\link[=findSeparateThresholdsModelled]{findSeparateThresholdsModelled()}}.}
}
\value{
A \code{touchstone_results} object containing the complete summarized
\code{data}, its complete scored CSM source, level-specific \code{thresholds}, the
resulting \code{fdr}, a compact \code{classificationSummary}, and the settings used.
Use \code{\link[=classifyCrosslinkResults]{classifyCrosslinkResults()}} to generate a classified and optionally
polished reporting table with recalculated support counts.
}
\description{
Starts from a scored CSM table, summarizes it at the requested proteomics
level, calculates level-specific inter- and intra-protein thresholds, and
generates a scaling-adjusted classification summary with \code{\link[=countDecoys]{countDecoys()}}. The
complete summarized target-and-decoy table and its thresholds are retained as
the canonical result. Classified and target-only tables can be generated
without storing duplicate copies; see Examples.
}
\details{
When \code{x} is a result from \code{\link[=trainCrosslinkScore]{trainCrosslinkScore()}}, \code{model = "selected"} uses
the linear recommendation. \code{model = "radial"} uses \code{recommendedRadial}, and
a numeric value uses that candidate index. When
the selected candidate's URP table and thresholds already match the requested
settings, those cached results are reused.
}
\examples{
\dontrun{
urp.results <- prepareCrosslinkResults(training, "urp")
accepted <- classifyDataset(urp.results$data, urp.results$thresholds)
reported <- removeDecoys(accepted)

radial.pp <- prepareCrosslinkResults(
  training,
  summarizationLevel = "protein-pair",
  model = "radial"
)
}
}
