% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distModFile.R
\name{euclideanHelper}
\alias{euclideanHelper}
\title{Measure crosslinked Euclidean distances across a results table}
\usage{
euclideanHelper(datTab, parsedPDB, exclude.self = TRUE)
}
\arguments{
\item{datTab}{Parsed CLMS search results. Must contain columns
\code{XLink.AA.1}, \code{PDB.chain.1}, \code{XLink.AA.2}, and \code{PDB.chain.2}.}

\item{parsedPDB}{A parsed Touchstone PDB table, usually generated by
\code{\link[=parsePDB]{parsePDB()}}. Must contain columns \code{chain}, \code{resno}, \code{x}, \code{y}, and \code{z}.}

\item{exclude.self}{Logical. If \code{TRUE}, candidate distances where the same
atom is linked to itself are excluded. This is useful when homologous
chains are supplied and a same-residue crosslink should be assigned to
different subunits. Default is \code{TRUE}.}
}
\value{
A data frame with \code{distance} and \code{chimera.coord} columns added.
}
\description{
Calculates C-alpha to C-alpha distances for crosslinked residue pairs in a
parsed CLMS results table. Chain identifiers may contain comma-separated
homologous chain alternatives, such as \code{"A,D,G"}. For each crosslink, all
possible chain combinations are evaluated and the shortest valid distance is
reported.
}
\seealso{
\code{\link[=parsePDB]{parsePDB()}}, \code{\link[=euclideanDistanceHomologous]{euclideanDistanceHomologous()}}
}
