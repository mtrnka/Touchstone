% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distModFile.R
\name{makeChimeraXpbondFile}
\alias{makeChimeraXpbondFile}
\title{Make a ChimeraX pseudobond file from crosslink coordinates}
\usage{
makeChimeraXpbondFile(
  datTab,
  pdb.code = NULL,
  threshold = 35,
  model.id = "#1",
  pass.color = "limegreen",
  viol.color = "red",
  radius = 0.5,
  dashes = 12,
  out.file = NULL
)
}
\arguments{
\item{datTab}{A data frame containing crosslink results. Must contain columns
\code{PDB}, \code{distance}, and \code{chimera.coord}.}

\item{pdb.code}{Optional character string giving the PDB code or file name
shown in the header comment. If \code{NULL}, the unique value from \code{datTab$PDB}
is used.}

\item{threshold}{Numeric distance threshold used to separate satisfied and
violating crosslinks. Default is \code{35}.}

\item{model.id}{ChimeraX model identifier to prepend to atom specifications
that do not already contain a model prefix. Can be supplied as \code{"#1"} or
\code{1}. Default is \code{"#1"}.}

\item{pass.color}{Color for crosslinks with \code{distance <= threshold}.
Default is \code{"limegreen"}.}

\item{viol.color}{Color for crosslinks with \code{distance > threshold}.
Default is \code{"red"}.}

\item{radius}{Numeric pseudobond radius. Default is \code{0.5}.}

\item{dashes}{Number of dashes for pseudobonds. Default is \code{12}.}

\item{out.file}{Optional output file path. If supplied, the pseudobond text is
written to this file.}
}
\value{
A character string containing the ChimeraX pseudobond file text.
}
\description{
Creates the text for a ChimeraX pseudobond (\code{.pb}) file from a crosslink
results table. Crosslinks with distances less than or equal to \code{threshold}
are colored with \code{pass.color}; crosslinks above \code{threshold} are colored with
\code{viol.color}. Self-links, where the same atom is linked to itself, are
removed.
}
\details{
The input table must contain columns named \code{PDB}, \code{distance}, and
\code{chimera.coord}. The \code{PDB} column is used as a sanity check. If multiple PDB
identifiers are present in \code{datTab}, the function stops and asks the user to
make separate pseudobond files for each structure.

Atom coordinates in \code{chimera.coord} should be formatted as ChimeraX atom
pairs, for example \code{"/A:123@CA /B:456@CA"}. These are automatically generated
when calling \code{measureEuclideanDistanceHomologous}. If atom specifications do not
already include a ChimeraX model identifier, \code{model.id} is prepended, giving
entries such as \code{"#1/A:123@CA #1/B:456@CA"}.
}
