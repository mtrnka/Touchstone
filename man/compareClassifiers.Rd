% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifierComparison.R
\name{compareClassifiers}
\alias{compareClassifiers}
\title{Compare classifier recovery across the FDR range}
\usage{
compareClassifiers(
  x,
  classifiers,
  summarizationLevel = "urp",
  model = "selected",
  scalingFactor = NULL,
  retainGroups = TRUE,
  polishing = NULL
)
}
\arguments{
\item{x}{A CSM data frame, a \code{touchstone_training} result, or a
\code{touchstone_results} object. Results objects use their complete retained
CSM source when available.}

\item{classifiers}{Character vector naming numeric score columns to compare.}

\item{summarizationLevel}{Proteomics level at which recovery is evaluated.
One of \code{"csm"}, \code{"urp"}, \code{"peptide-pair"}, \code{"protein-pair"}, or
\code{"module-pair"}.}

\item{model}{Model selected when \code{x} is a training result; passed to the same
resolver used by \code{\link[=prepareCrosslinkResults]{prepareCrosslinkResults()}}.}

\item{scalingFactor}{Decoy-database scaling factor. By default this is read
from \code{x} when available, otherwise from \code{\link[=setDecoyScalingFactor]{setDecoyScalingFactor()}}.}

\item{retainGroups}{Retain existing groups during summarization.}

\item{polishing}{Optional named polishing rules accepted by
\code{\link[=classifyCrosslinkResults]{classifyCrosslinkResults()}}. These filters are applied once, before the
classifier-specific summarization, without applying an FDR threshold.}
}
\value{
A tibble with classifier, summarization level, crosslink class,
native score threshold, calculated FDR, and recovered target hits. The
polishing audit is retained as a \code{polishingAudit} attribute.
}
\description{
Builds on \code{\link[=generateErrorTable.sep]{generateErrorTable.sep()}} to evaluate several score columns on the
same CSM evidence. Each classifier independently chooses the representative
match at the requested proteomics summarization level, after which target
recovery and decoy-estimated FDR are calculated over its score range. This
does not fit or retrain a model.
}
\details{
The returned long table can be inspected directly or passed to
\code{\link[=plotClassifierComparison]{plotClassifierComparison()}}. Its \code{threshold} column remains on each
classifier's native scale; \code{fdr} and \code{hits} provide the scale-independent
comparison.
}
\examples{
\dontrun{
comparison <- compareClassifiers(
  scored.csms,
  classifiers = c("SVM.score", "Score.Diff", "experimental.score"),
  summarizationLevel = "urp",
  polishing = list(minIons = 3)
)
plotClassifierComparison(comparison, targetER = 0.01)
}
}
